{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "regions": ["icn1"],
  "installCommand": "pnpm install --frozen-lockfile --store-dir /tmp/.pnpm-store",
  "buildCommand": "pnpm run build && rm -rf .pnpm-store",
  "outputDirectory": ".",
  "rewrites": [
    { "source": "/mcp", "destination": "/api/mcp" },
    { "source": "/", "destination": "/api/mcp" }
  ],
  "functions": {
    "api/mcp.ts": {
      "maxDuration": 15
    }
  }
}
